# Assumptions

- The remote has no `origin/main`; Phase 8A starts from authoritative remote default `agent/phase-1-foundation` at `734c366`.
- The canonical JPEG is preserved unchanged; no derivative is claimed until its fidelity can be verified against that source.
- Prototype fixtures are fictional and contain no credentials or real tenant data.
