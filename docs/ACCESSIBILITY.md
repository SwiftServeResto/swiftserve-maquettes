# Accessibility

The prototype targets WCAG 2.2 AA: semantic landmarks/headings, text labels with every status, visible focus rings, keyboard-accessible controls, reduced motion, 44/48 px targets, responsive zoom, live alert roles and no color-only meaning. Automated Playwright checks cover routes, overflow, keyboard controls and responsive projects.
