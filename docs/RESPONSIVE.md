# Responsive behavior

At 1440 px the Web shell uses full sidebar navigation. At 1024 px it contracts to a compact rail. Staff tablet layouts use the same rail model; phone layouts use fixed bottom navigation with 48 px targets. Content collapses from two columns to one below 600 px, preserves scroll ownership and prevents horizontal overflow.
