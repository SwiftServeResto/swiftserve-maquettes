# Prototype interactions

The control panel simulates role, workspace, theme, connection and screen state. Galleries support navigation and state selection; operational cards simulate canonical tables, orders and kitchen status. All controls are explicitly non-authoritative and use local fixtures.

History navigation, refresh, and back/forward behavior retain clean URLs. The canonical route manifest drives both Vue Router and generated Pages entry points, avoiding redirect loops or hash routing.
